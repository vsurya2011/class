services:
  - type: web
    name: attendance-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: classdb
          property: connectionString
      - key: EMAIL_USER
        value: vv708539@gmail.com
      - key: EMAIL_PASS
        value: osjx pfrw sjtc cglf
      - key: RECEIVER_EMAIL
        value: krishnasurya2011@gmail.com

databases:
  - name: classdb
    plan: free
    databaseName: attendance_db
    user: admin